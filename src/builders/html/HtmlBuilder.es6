var HtmlBuilder;
(function(){

	HtmlBuilder = class_create({
		constructor (template, opts) {
			this.template = template;
			this.opts = opts;
		},

		process () {
			var $ = require('cheerio');
		}
	});

}());