var IPackageBuilder;
(function(){

	IPackageBuilder = class_create({
		constructor () {

		},

		add (resource) {

		},

		getOfType (type) {
			return class_Dfr.run((resolve, reject) => {

			});
		},
		saveOfType (type, targetUri) {
			return class_Dfr.run((resolve, reject) => {

			});
		}
	});

}());
